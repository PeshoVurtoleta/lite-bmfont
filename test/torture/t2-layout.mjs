/** T2 -- the layout-buffer abuse matrix. Registered empty in M0; filled by M2. */
export const TODO = 'M2';
export function run() {
    process.stderr.write('torture: TODO -- T2 layout is registered but empty (filled by M2)\n');
}
