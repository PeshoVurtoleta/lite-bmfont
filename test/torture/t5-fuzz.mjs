/**
 * T5 -- the differential fuzz tier vs an ALLOCATING reference renderer.
 *
 * NOT this session's. The 50k-tuple conservation differential the brief cited is
 * a statement about the conservation LAW, which lives in T0 (ROADMAP :215 -- "the
 * law is O(glyphs) and belongs in T0 and T5") and T0 delivers it (t0-laws law 1,
 * 50,000 tuples). The tier T5 actually specifies is a whole allocating reference
 * renderer whose first customer is M4's per-line Y drift (ROADMAP :1265, :1290,
 * :1298). Registered empty until then; a stub claiming a debt the plan does not
 * pay is the same class of lie as the old oracleAdvance comment.
 */
export const TODO = 'M4';
export function run() {
    process.stderr.write('torture: TODO -- T5 fuzz is registered but empty (filled by M4)\n');
}
