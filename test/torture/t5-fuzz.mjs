/** T5 -- the differential fuzz tier vs the oracle. Registered empty in M0; filled by M2. */
export const TODO = 'M2';
export function run() {
    process.stderr.write('torture: TODO -- T5 fuzz is registered but empty (filled by M2)\n');
}
