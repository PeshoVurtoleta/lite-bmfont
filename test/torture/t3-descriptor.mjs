/** T3 -- the descriptor/constructor abuse matrix. Registered empty in M0; filled by M3. */
export const TODO = 'M3';
export function run() {
    process.stderr.write('torture: TODO -- T3 descriptor is registered but empty (filled by M3)\n');
}
