/** T4 -- the drawFast numeric / magnitude-door wall-clock tier. Registered empty in M0; filled by M1. */
export const TODO = 'M1';
export function run() {
    process.stderr.write('torture: TODO -- T4 numeric is registered but empty (filled by M1)\n');
}
